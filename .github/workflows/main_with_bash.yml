name: CI_with_bash

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2

    - name: Run CI Script
      run: |
        chmod +x ./run_ci.sh
        ./run_ci.sh
