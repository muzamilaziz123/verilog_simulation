name: CI_bash

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v2


    - name: Source and Run Script
      shell: bash
      run: |
        source run_ci.sh

